{
  "$comment": [
    "The Ashmorrow realm's launcher manifest. The website serves this at",
    "/api/launcher/manifest, filling the realm address in from its own",
    "environment so this file never has to know a deployment's hostname.",
    "",
    "client.files is EMPTY, and that is the honest current state: hashes have",
    "to be measured against a real 3.3.5a client, and nobody has done that",
    "yet. Until they are, the launcher checks structure and build number, and",
    "says in its own interface that it has nothing to compare contents with.",
    "",
    "To fill it in, from a machine that has a client:",
    "",
    "  cargo run --manifest-path launcher/core/Cargo.toml \\",
    "        --bin ashmorrow-manifest -- hash /path/to/WoW-3.3.5a",
    "",
    "then paste build, version, locales and files in here, and record where",
    "the copy came from in measured_from. The output is paths, sizes and",
    "hashes - facts, safe to commit and publish. No client bytes leave the",
    "machine that runs it, and none are ever committed here."
  ],
  "schema": 1,
  "realm": {
    "name": "Ashmorrow",
    "address": "127.0.0.1",
    "auth_port": 3724,
    "world_port": 8085
  },
  "client": {
    "build": 12340,
    "version": "3.3.5a",
    "locales": [],
    "measured_from": "",
    "files": []
  },
  "patches": [],
  "$comment_runtime": [
    "Free software the launcher installs into its own Wine prefix so a Linux",
    "player does not have to assemble a runtime by hand. Not ours and not",
    "Blizzard's - DXVK is zlib-licensed and freely redistributable, which is",
    "what separates this from a client file.",
    "",
    "The size and hash below were measured from the release itself and the",
    "archive was installed into both a 32- and a 64-bit prefix by",
    "launcher/core/tests/real_dxvk.rs. To move to a new DXVK version, run that",
    "test against the new tarball; it prints the size and hash to paste here."
  ],
  "runtime": [
    {
      "id": "dxvk",
      "kind": "dxvk",
      "version": "2.7.1",
      "size": 10310666,
      "hash": "5b03e26c028ae0d3494beeba85314a3613cdb416834e053d37ebbd7dad8f5fe1",
      "url": "https://github.com/doitsujin/dxvk/releases/download/v2.7.1/dxvk-2.7.1.tar.gz",
      "licence": "Zlib",
      "summary": "Direct3D 9 over Vulkan. Without it a 2010 D3D9 game under Wine is slow, or a black window."
    }
  ],
  "launcher": {
    "minimum_version": "0.1.0",
    "latest_version": "0.1.0"
  }
}
